# SCMOS AI Implementation Log

## 2026-09-07 — Phase D: permanent run/tool audit

Implemented locally at the user's explicit request. Reused the authorized existing OpenAI configuration contract, without exposing/copying its key or making provider requests. OpenAI Docs informed the separation of local audit minimization from provider retention; no provider Zero Data Retention claim or configuration change. Reference: [OpenAI data controls](https://developers.openai.com/api/docs/guides/your-data).

### Discovery / additive migration

Inspected EF 10.0.11 / SQL Server mappings, existing audit_events / ai_tools / approvals, and live schema metadata/history using SELECT only. Initial live history ended at CustomerRateKind; final recheck ended at LineIntegration. Exact dbo.ai_audit_logs name remained absent. No business rows were read or changed on production. Wrote the database proposal before implementing the new schema. Concurrent source advanced 919d802 to 0f6acb0 (Oil Rate / DieselPrices); preserved that work and its shared snapshot.

Added Data/AiAuditLog.cs and migration 20260907092459_AiExecutionAudit. One append-only table, unique run+sequence and time indexes. Current one-tool run is represented by four immutable events; no duplicate conversations/approvals schema. Down always refuses deletion: rollback is disable AI + revert application while retaining audit evidence. No migration/deploy/Git push or AI activation by this task. Review the full pending migration chain before release, including concurrent DieselPrices and snapshot coupling.

### Runtime / API

- SqlAiExecutionAudit: actual table-shape readiness probe, per-write five-second bound, isolated context, serializable transaction, SQL retry with exact idempotent event replay; no saving unrelated tracked business entities and no silent in-memory fallback.
- AiAuditRules: server-controlled vocabulary and bounded metadata; rejects identity/scope/model/tool changes, orphan/reordered events, conflicting retries, invalid counts and references. Stores safe view/limit, configured model, caller/scope, low risk, not-required approval, source keys and usage; no prompt/search text, raw tool arguments, provider output, hidden reasoning or free-form error body.
- OperationsAgent: commits audit before provider/read and before releasing evidence; records sanitized provider/source errors, awaits cancellation cleanup with an independent token, withholds evidence on any audit write failure. Tool ID is generated by SCMOS, not copied from the provider.
- AiAuditReader and GET /api/ai/audit plus /api/ai/audit/{runId}: existing ViewAudit + internal recognized roles, bounded cursor paging, no-store, no mutation routes. Incomplete traces remain visibly incomplete after two minutes, not rewritten. User IDs and source references remain protected operational metadata, not anonymous data.
- Existing best-effort AuditService, approval routes, document extraction, business permissions, operations/rate rules and UI were not replaced. Preflight rejections/mock requests/legacy extraction are explicitly outside the new durable Operations run trail.

### Verification

- Offline AI suite: 195 checks passed (152 existing + 43 Phase D).
- Isolated SQL + HTTP suite: 232 checks passed. Real SQL migration Up, new-context persistence, concurrent replay, actor/scope checks, pagination, missing schema, actual constraint-injected write failure at each of four boundaries, cancellation cleanup and non-destructive downgrade refusal. Provider/source are fake fixtures. Temporary test databases removed; no production database writes.
- API Release build with warnings as errors: 0 warnings/errors. EF has-pending-model-changes: none.
- Node tests: 380 passed. TypeScript: passed. Quotation checks: 21 passed. Lint: 0 errors and 3 pre-existing warnings (layout font, Chrome image, CargoForm image).
- Initial default LocalDB startup failed before database creation. Created a separate SQL Server 2019 LocalDB instance for verification; no changes or stop/delete operation to the user's default instance. A first fault-injection attempt used triggers, which interfered with EF OUTPUT for every insert; replaced the test-only injection with event-specific CHECK constraints and verified all intended boundaries.

At handoff the dedicated ScmosAiAuditCheck_20260907 instance was verified to contain no user databases, then stopped and removed. Only this turn's temporary SQL resources were removed; the user's default instance/databases were left intact.

Generated and reviewed migrations/20260907092459_AiExecutionAudit.sql (idempotent, LineIntegration → AiExecutionAudit only). It creates the audit table/indexes and records its EF migration ID; no existing business-table DDL/DML. The generated script was not applied to production. See the database release checklist.

Next: Phase E UI, with production migration/release, retention policy and live provider/source smoke tests still gated separately. Permanent storage is implemented and SQL-tested locally; it is not yet installed/activated on production by this task.

## 2026-09-07 — Phase A: source discovery

### Request and scope

User supplied SCMOS AI Agent Platform V1 master specification. First execution is inspection of the existing production-oriented app, architecture/domain mapping and a checkpoint. No AI implementation or migration in this entry.

### Snapshot

- Repository: `D:\Leschaco\Dashboard\scmos-report-dashboard`
- Branch: `azure-dotnet-migration`
- Discovery began at `5350264`; another active workstream committed `9b31ab49aa86a148fcb0d65deca0f4f6b06c36dc` during inspection.
- At the recorded checkpoint, branch was five commits ahead of the tracked remote. Do not equate local HEAD with Production.

### Files added

- `docs/ai/SCMOS_CURRENT_ARCHITECTURE.md`
- `docs/ai/SCMOS_DOMAIN_MAP.md`
- `docs/ai/SCMOS_AI_IMPLEMENTATION_LOG.md`

### Files modified / database / API

- Phase A modifies no existing application files.
- Database changes: NONE. No new migration generated/applied, no seed, no production row read/write.
- API changes: NONE. No OpenAI request, provider setup or external integration activation performed.
- Deployment: NONE. No push or workflow dispatch.

### Evidence / verification

- Read package/config conventions, Next entry/proxy, C# registration/auth, all endpoint family names, selected services/rules, EF mappings and 33 migration filenames.
- Model inventory: 48 mapped tables, including existing `ai_tools`, `approvals` and `audit_events`.
- Azure read-only metadata: two App Services, one plan, Storage, SQL server and databases in `rg-scmos`; active `scmos` DB Online. Live table/column schema and applied migration history NOT checked.
- Environment-file inspection showed setting names only, not secrets.
- Reviewed official OpenAI function-calling documentation for application-owned tool execution and strict schema guidance; no SDK change or model selection made.

### Baseline from the immediately preceding task (not a release certification)

- At `5350264`, 320 Node tests passed; lint had three warnings; TypeScript passed.
- Integration status-race fix subsequently passed 15 focused tests (seven new), and an isolated Edge headless test with mocked localhost API checked late success/failure, retry, return visit and unmount. This work was completed before the AI specification, and another workstream included those files in `9b31ab4`.
- Temporary test route/script generated for that test were removed; no test business records were written.
- During concurrent Cargo Receipt edits the combined suite temporarily failed on old exports (`receiptLine`, `vehicleLine`), and lint reported a redundant regex escape. Those files subsequently changed/committed. These results are historical, not a claim that current HEAD still fails.
- TypeScript passed at the interim snapshot. Full tests/lint/build need rerunning against the exact eventual implementation/release SHA; no production readiness claim.

### Key decisions / issues

1. Reuse .NET `AiGateway` and existing routes/approval data, rather than implementing a competing TypeScript gateway/database.
2. Generic AI Allow is not caller authorization. Scope/per-tool capabilities must be explicit before real read handlers are attached.
3. Current risk endpoint can treat blank restricted owner as unscoped; current approval listing exposes payloads without row scope. Record policy decisions before changing existing access.
4. Existing applied approval state is not execution. No high-risk tools will be wired automatically.
5. Existing audit service is best effort and not full AI run telemetry. Persistent run/tool auditing is required before activating live tools.
6. Billing has no invoice source; no fabricated billing metrics. Preserve current KPI/training rules.
7. API workflow can deploy and migrate on a server-code push to this branch; release authority must be checked before pushing.
8. Provider credential choice is a prerequisite for Phase B API-backed work. Discovery documentation does not authorize using or creating a key.

### Rollback notes

This phase adds documentation only. Reverting just these documentation additions does not change application behavior or data. Preserve unrelated changes from other workstreams. For future AI migrations, prefer disabling AI and retaining new audit tables over destructive downgrade/drop operations.

### Next checkpoint

Phase A source discovery complete. See architecture section 12 for the recommended B/C sequence and later phases. Resolve provider credential choice and required read-scope policy before beginning API-backed implementation. No permission changes, commercial writes or external communications without explicit approval.

## 2026-09-07 — Phase B: additive foundation

### Authority / snapshot

User explicitly authorized reuse of the existing Azure API-side key and continuation of Phase B. No local secret write or live provider request was needed. Work started at `6ffbea9`; concurrent unrelated work advanced HEAD to `3a36786` and introduced CustomerRateKind changes/migration during testing. Those changes are not part of this AI task and were preserved.

### Files added

- `server/Scmos.Api/Ai/`: options, typed contracts, schema validation, provider abstraction, OpenAI/mock providers, specialist registry, tool registry, scope/permission policy, bounded run limiter, orchestrator and DI registration.
- `server/Scmos.Api/Endpoints/AiChatEndpoints.cs`: additive status/chat routes.
- `tests/Scmos.Ai.Checks/`: standalone executable checks using fake identity, in-memory SDK transport and a loopback-only host without SQL.
- `docs/ai/SCMOS_AI_ARCHITECTURE.md`, `SCMOS_AI_TOOLS.md`, `SCMOS_AI_AGENTS.md`, `SCMOS_AI_SECURITY.md`, `SCMOS_AI_DATABASE.md`.

### Files modified

- `Program.cs`: register foundation and map its routes, without changing existing setup.
- `Services/AiGateway.cs`: additive status/chat entry points; legacy constructor/methods unchanged.
- `.github/workflows/api.yml`: add secret-free foundation checks to the existing build job. No workflow dispatched and no trigger/deploy behavior changed by this task.
- This implementation log.

### Database / API / external changes

- Database changes: NONE for Phase B. No migration created/applied, seed or production data access.
- API: add `GET /api/ai/status` and `POST /api/ai/chat`. Existing tools/approval/extraction endpoints unchanged.
- Azure/configuration: NONE. Existing `OpenAI__ApiKey` is consumed by the new adapter through existing server configuration when later activated; not copied or exposed. No new model or SDK dependency.
- Deployment: NONE. No commit, push or release initiated by this task.

### Verification

- API Release build with warnings-as-errors: passed, 0 warnings/errors.
- Isolated AI checks: 93 passed (authorization/scoping, routing, flags, schema, forbidden/high-risk tools, mock restriction, limiters, SDK request/output handling, safe errors, cancellation and HTTP input boundaries).
- Existing .NET quotation checks: 21 passed in arithmetic-only mode; no `--local-db` option or database connection was used.
- Existing Node tests: 346 passed; TypeScript: passed. Lint: 0 errors, 4 warnings in existing frontend files (layout fonts, Chrome/CargoForm images, Chemours effect dependency). Those frontend files were not changed by Phase B; concurrent frontend work continued after this verification snapshot.
- An initial Debug build encountered an already-running user API executable lock. Its process was not stopped; verification used Release with `UseAppHost=false` instead.
- No live OpenAI response, Azure deployment, real job retrieval or production smoke test claimed. Frontend Control Tower is not a Phase B deliverable.

### Deliberate limits / next phase

Phase B provides B1–B8 foundation. Business tool definitions have no handlers and real chat reports `not_connected` before contacting the provider. Development mock is explicitly labeled, with no source reads or fake metrics. Read execution/risk calculation and persistent audit tests remain Phase C/D, not falsely counted as implemented tests. The live-tool gate requires both scoped source adapters and durable audit; flags alone cannot enable writes.

### Rollback

Keep all AI flags off; no database rollback is needed. If reverting source, remove only these foundation registrations/routes/methods, added files and CI step, preserving shared-worktree changes from other tasks. Do not reset the branch or revert unrelated migrations. Review the exact release diff before any future push, because this branch's API workflow can deploy and migrate automatically.

## 2026-09-07 — Phase C: read-only Operations agent

### Authority / scope

User requested Phase C immediately after the Phase B handoff. Continued the same approved server-key configuration; no new credential, secret copy, live provider call, Azure setting change, deployment or production data access. Started against df384ff; unrelated work advanced HEAD to 3313f09 during this task (LINE storage). Those changes were preserved and are not Phase C database changes.

### Added

- Data/JobsRepository.Analysis.cs: scoped, no-tracking read with repository-side JSON projection; independent blank-owner refusal.
- Ai/Operations/OperationsReadService.cs: source adapter, typed evidence, bounded results, existing risk/tab rules and one Thai reference date.
- Ai/Operations/OperationsAgent.cs: one authorized model-selected read, server-composed answer, strict audit prerequisites, sanitized failures.
- Ai/IAiExecutionAudit.cs: metadata-only contract and unavailable-by-default production registration.
- tests/Scmos.Ai.Checks/OperationsChecks.cs: source/rule/scope/audit and gateway HTTP tests using synthetic fixtures.

### Modified

- Phase B tool schema/registry: constrained view argument and scoped read handlers; no role/catalogue permission changes.
- Contracts, orchestrator, DI, chat endpoint: evidence payload and separate Connected/AuditReady/LiveToolsReady metadata.
- Mock wording, existing check runner and CI step label. Core mock remains clearly labeled and performs no source reads.
- Architecture, agents, tools, security, database and implementation documentation.

### Behavior / decisions

1. The model chooses a single permitted query; business rows are not sent to it. Server code supplies counts and reasons. Unsupported/free-form model answers produce clarification, not invented facts.
2. Risk uses MonitorRules.Judge, not the older RiskService whose date/scope caveats were recorded in Phase A. The risk_today window is explicitly overdue through the next two days; scheduled-today is a separate view. No risk score/OTD redefinition.
3. SQL applies restricted owner scope first; the read service checks it again before any count, including malformed rows. Identity and owner cannot come from model arguments.
4. Total counts cover all matched scoped rows, independent of the 1–50 evidence cap. Invalid dates/rows are visible quality indicators. One source read per tool; no repeated per-job queries. Production latency remains unmeasured.
5. The default durable audit dependency remains unavailable until Phase D. Connected Operations code is tested, but default live requests stop with audit_not_ready before SQL/provider access. No bypass flag or production in-memory fallback.

### Verification

- Foundation + Operations: 152 checks passed, including the target high-risk question over fixtures, generated SQL ownership predicate, private-field removal, Thai midnight/year boundary, rule parity, full totals vs caps, unknown/write/forged tools, audit failures before/after read, cancellation, source failure and signed-in HTTP scope/gate behavior.
- API Release build with warnings-as-errors: passed, 0 warnings/errors.
- Existing Node tests: 373 passed. TypeScript: passed. Existing quotation checks: 21 passed in arithmetic-only mode.
- Lint: 0 errors, 3 existing warnings (layout custom font and Chrome/CargoForm images). No frontend file was changed by Phase C.
- No live provider validity/quota test, real SQL query, production latency test, deployment or production smoke test claimed. All provider requests used in-memory transport or test fixtures; HTTP hosts were loopback only, separate from SCMOS startup/scheduler.

### Database / rollback / next

No migration or business-data write. Unrelated concurrent LINE migrations are outside this task. Rollback is code-only: keep flags off, remove Phase C registrations/paths while preserving Phase B and other workstreams. Do not reset the shared branch or reverse another task's migration.

Phase C implementation complete; proceed to Phase D persistent AI run/tool auditing before activating real reads. Control Tower UI remains Phase E. Recheck the exact shared-worktree release diff before push because the existing API workflow can deploy/migrate automatically.

## 2026-09-07 — Phase D durable audit (preceding handoff)

Added SqlAiExecutionAudit, metadata-only ordered events, scoped read endpoints, AiAuditLog and additive migration 20260907092459_AiExecutionAudit. The audit reader distinguishes incomplete from succeeded; migration Down refuses to discard evidence. See SCMOS_AI_DATABASE.md, SCMOS_AI_SECURITY.md and tests/Scmos.Ai.Checks/README.md for the persistence contract and the preceding isolated SQL verification. Default tests now contain 195 checks; the opt-in isolated SQL/HTTP path contains 232. Phase E reran the default checks only, not the isolated database checks.

## 2026-09-07 — Phase E AI Control Tower

User requested Phase E, then asked to continue the unfinished work. Implemented locally against the current shared workspace, starting at 65442cf. Other workstreams advanced HEAD through c1ed268 (OTD coverage); their changes were preserved. No subagents, deployment, push, production migration, Azure configuration change or live provider call was used.

### Added / integrated

- AI Control Tower sidebar item and authenticated /ai-control-tower entry, using existing Home/Easy Auth behavior; normal Home landing preferences remain unchanged.
- Morning Brief and Priority Queue from existing Dashboard endpoints, separate scope/window labels, topic-only Risk Summary and unavailable-data handling.
- Explicit Operations AI form with prompt chips, Ctrl/Cmd+Enter, length limit, duplicate-request guard, cancel and structured evidence/source links.
- AI Activity list/detail, cursor navigation, status timeline and source references using existing ViewAudit capability.
- Responsive scoped CSS, shared ZoomBox for evidence, strict client contracts and safe error mapping. No new backend/migration/permission or business scoring code.
- Sixteen focused Node tests, synthetic fixtures and an isolated loopback-only Next preview wrapper. ESLint/TypeScript ignore only that wrapper's generated build output.
- Source mapping, interaction, limitations, release and manual QA documentation in SCMOS_AI_CONTROL_TOWER.md.

### Verification / qualification

- All 410 Node tests passed after integrating shared ZoomBox (includes concurrent workstreams; E adds 16).
- AI regression: all 195 default fake-provider/no-SQL checks passed. Existing EF1002 warning remains in the Phase D isolated SQL helper.
- Production build succeeded with the dynamic /ai-control-tower route; TypeScript passed. Lint source checks retain the three pre-existing font/image warnings.
- Local fixture HTTP page returned 200 and rendered Morning Brief, Ask SCMOS AI and AI Activity. Fixture identifiers were absent from built client chunks.
- Browser automation failed twice during initialization due to Windows helper/kernel errors. Interactive and visual QA remain pending. No screenshot/mobile/real-user interaction verification is claimed.
- No real OpenAI call, live SQL read, production benchmark, commit or deployment was performed by E.

### Next / rollback

Complete the documented browser checklist before release, then separately review shared changes and pending migrations before deployment/enablement. Vendor/KPI specialists and write/approval features remain later phases. E rollback is UI-only and must preserve the Phase D audit table/history and other workstreams.

## 2026-09-08 — B–E pre-deployment QA continuation

The user approved QA/preparation and then requested continuation. No push, workflow dispatch, deployment, migration, AI enablement, new key, live provider call or business-data mutation was performed. Resumed at aaf2b6f; unrelated work advanced to bfa44c9 and was preserved. The candidate remains a shared dirty workspace, not an immutable release.

### Fix and verification

- Reproduced EF snapshot/runtime drift: AiAuditLog was absent from the latest snapshot. Added a no-connection regression and restored only its original generated 145-line entity block. The applied Diesel migration/designer and Audit migration Up/Down were not rewritten; no new migration was generated.
- API Release build: 0 warnings/errors. AI default checks: 196 passed. EF model-change check: passed. The SQL opt-in suite was not rerun (historical Phase D result: 232 passed).
- Web: 410 Node tests passed, TypeScript passed, production build passed with /ai-control-tower. Lint: 0 errors and the same 3 existing font/image warnings. Quotation arithmetic regression: 21 passed.
- Added optional tests/ai-control-browser.mjs and expanded the isolated fixtures with operator/carrier identities and off-page source jobs. Existing Playwright plus installed Edge headless ran 15 scenario groups successfully, without installing dependencies or using a signed-in browser profile.
- Visually inspected desktop, tablet, mobile, mobile answer and desktop Audit/evidence screenshots. Verified mobile width containment. Early partially visible sidebar screenshots were captured during its existing transition; waiting for the closed position resolved this without changing application CSS.
- Browser proof covers explicit keyboard submission, duplicate/cancel guards, evidence and source drawer, Audit selection/cursor, permissions, error/empty/mock/disabled states, mobile form and carrier exclusion. It is not actual Entra/SQL/provider integration.

### Production boundaries

The successful metadata check on 2026-09-07T15:01:05.3788119Z found 36 applied SQL migrations, latest 20260907093954_DieselPrices, only AiExecutionAudit pending and Audit storage absent. The provider key was present without revealing its value; no AI app-setting overrides were returned (not proof of effective flags).

The attempted SQL/config refresh on 2026-09-08 was blocked by automatic approval before execution due to Production credential use. No bypass was attempted. A narrower resource-only query succeeded: Web/API Running and no Web deployment slots. No fresh SQL/config result is claimed.

Published the checkpoint in SCMOS_AI_PREDEPLOY_QA.md and updated Control Tower, architecture, database, security and test documentation. Next actions require a reviewed release candidate, explicit credential-use approval for a fresh schema/history check, an integration-environment decision and actual authenticated source/provider/Audit smoke tests. Preserve durable audit on rollback.

## 2026-09-08 — explicitly approved read-only SQL retry

The user explicitly approved reading/using the Azure App Settings connection credential solely for read-only schema/migration checks. Retrieved that setting without displaying it, validated the exact scmos target and attempted a bounded metadata-only SQL connection. The password was passed through process environment and restored in finally; no secret file was created.

SQL rejected the connection because the current observed client IP was not allowed. No metadata or business-row query ran. Resource-only firewall inspection at 2026-09-08T04:36:41.1513595Z confirmed public network access enabled, four server rules and no matching range. No firewall edits, migration, deployment or alternate access route were attempted.

Updated the QA/database checkpoint: credential-use permission is resolved; network access is the current blocker. Any temporary single-IP firewall addition and subsequent removal needs separate approval. The last successful SQL evidence remains the dated 2026-09-07 check.

## 2026-09-08 — approved temporary access; SQL checkpoint completed

The user explicitly approved a temporary firewall rule only for 49.229.138.124 and its immediate removal after inspection. Verified four existing rules and no match for that IP. Created only codex-schema-20260908-0439-8c7a1d with identical start/end IP, retaining all existing rules.

At SQL timestamp 2026-09-08T04:40:58.2671664Z, the exact scmos database (compatibility 170) reported 36 applied migrations versus 37 local, latest applied 20260907093954_DieselPrices, only AiExecutionAudit pending, no unknown applied IDs and no dbo.ai_audit_logs object. The connection had database VIEW DEFINITION. Queries were limited to EF history and sys metadata; no business rows or database mutations.

Finally cleanup restored the password environment and removed only the new rule. At 2026-09-08T04:41:03.5053715Z, a fresh firewall listing confirmed its absence, four remaining rules and exact equality of all original names/address ranges. No credential was displayed or saved; no migration, deploy, provider call or AI flag change occurred.

Updated the database/QA report with these fresh facts. The read-only schema/history task is complete and temporary access is closed. Audit migration execution, backup/restore readiness, exact release review and authenticated integration remain separately approved work.
