# SCMOS AI platform — Phase 6, first Engineering read

Implemented locally 21 September 2026 and merged onto the v2.7.70 production code for release. **Partial Phase 6.** Phase 5's newer Document & Invoice Agent reads paperwork, billing timeliness and expiry under SCMOS's existing rules. It does not compare invoice amounts: there is no authoritative invoice ledger or approved rate/terms source, so amount reconciliation remains deferred.

## Boundary

- `engineering-agent` has one read-only tool, `query_repository`, behind `AI:EngineeringAgentEnabled` (default `false`). Only an Administrator may use it. The UI choice is hidden when the server omits the agent, but the API enforces the role independently.
- The server fixes the public repository to `fose-cloud/SCMOS`. The model may select only `open_issues`, `open_prs` or `recent_commits` and a limit of 1–20. It cannot provide a repository, URL, ref, command, mutation or credential. The server uses GET only and disables redirects.
- GitHub's issues listing includes pull requests, so the issue view excludes those entries. Each response contains only number/SHA, title/commit subject, state, timestamp and a server-constructed canonical public link. Issue bodies may be present in GitHub's REST response; they are never projected into tool evidence, audit or model context. Diffs and source files are not fetched.
- Results are limited to the first API page, with at most 20 displayed rows. `total` and `returned` are counts of that sample, **not** the repository-wide totals. Titles are untrusted display data. The provider selects the read but never receives the returned titles, and its free-form prose is not used as the factual summary.
- Existing AI identity, capability check, typed schema, read policy, per-run budget and durable audit apply. Audit failure withholds evidence. No PR/issue creation, comment, merge, push, deploy, code execution, source-file read or SCMOS business-data write is in this slice.

## Verification and release boundary

Offline tests cover the fixed GET path, PR filtering, Administrator-only routing, default-off flag, typed tool rejection, malicious title/body isolation, canonical-link validation and audit failure. Client parsing rejects arbitrary links, body fields, invalid counts and agent/evidence confusion. On the integrated v2.7.70 base, 654 offline AI checks and 587 Node tests passed; the API Release build passed with warnings treated as errors. No live OpenAI invocation was part of these checks.

Release must confirm the live GitHub endpoint, successful API/Web deployments, an authenticated Administrator read and a non-Administrator denial. The user separately authorized Production enablement; the flag must remain off until the deployed code and health checks pass. No migration is needed for this read-only slice. Future phases must address GitHub API rate limits, paging, full-history totals and any repository write approvals separately.

## Second increment — a bounded, read-only read of the source (22 September 2026, v2.7.73)

On the department lead's word of 21 September: "อ่านซอร์สแบบจำกัดและอ่านอย่างเดียวในรีโป SCMOS ก่อน ไม่ให้ AI รันคำสั่ง แก้ไฟล์ หรือ Deploy เอง". The first read answered from titles; a question about a cause needs the code. This increment lets the same agent, for the same Administrator, read the repository's own source — and nothing else.

**What it does.** A second tool, `read_source`, behind its own switch `AI:EngineeringSourceEnabled` (default `false`, beside `AI:EngineeringAgentEnabled`): `mode=list` lists one directory; `mode=file` reads one window of one file's lines (200 by default, 400 at most, numbered), at the server's fixed repository and ref — `fose-cloud/SCMOS` at `azure-dotnet-migration`, the branch production is built from. One question may take up to **four** such reads; each is its own audited step (`tool_started`/`tool_completed` step *k*, the 1D topology, the first run to use more than one), and what was read is carried to the next call as excerpts labelled untrusted data. After the reads, or when the model has enough, it answers in words — สาเหตุที่น่าจะเป็น with the file and lines it rests on, ข้อเสนอการแก้ไข as text, สิ่งที่ยังไม่แน่ใจ — and that text is returned on the answer as `source.analysis`, under the label **การวิเคราะห์ของโมเดล — ยังไม่ได้ตรวจสอบ ทดสอบ หรือแก้ไข**. The summary line stays the server's: what was read, how many times, that the analysis is the model's, that nothing was run.

**What it may read — `SourcePolicy`, applied before any request leaves.** `app/`, `server/Scmos.Api/`, `tests/`, `docs/` and a few named top-level files (`README.md`, `package.json`, …); source, test and document extensions only. Never: `Migrations/`, `.github/`, `node_modules/`, `.next/`, `bin/`, `obj/`, `.claude/`, `Properties/`; any `appsettings*`, `.env*`, `launch.json`, `secrets*`, key, certificate or store file; a path that climbs (`..`), starts at the root, holds a control character or exceeds 200 characters; a file over 256 KB. A listing shows only entries the policy would let it read, so a settings file is not even named. A refused read is a step that says why — in the server's words — and the run goes on; the audit records it with no evidence. Everything read passes `SourcePolicy.Scrub`: anything shaped like an API key, a GitHub token, an AWS key, a bearer token, a JWT or a `password=` / `token=` value is replaced by `[REDACTED]` before it reaches the model or the person — the tracked files hold none, and this is the belt beside the braces.

**What it cannot do.** There is no tool that runs, edits, commits, pushes, comments, merges or deploys — none exists to be offered, so none can be selected, and the schema refuses a `ref`, `repo`, `url` or `command` argument. The provider's request cannot name a URL; the HTTP source is GET-only at one host, redirects disabled, and refuses a path the policy would refuse before requesting. A fifth read is refused; a metadata read after a source read is refused (a run answers from one or the other); a tool call after the model was offered no tool is refused. An instruction inside a file is a comment in a file: the system prompt says so, the excerpts go in as a user turn labelled untrusted data, and the model's text goes only into `analysis`, never into the summary and never into any action.

**Files created** — `Ai/Engineering/SourceReadService.cs` (`SourcePolicy`, `SourceReadService`, `SourceStep`, `SourceAnswer`, `SourceRefusedException`, `SourceReadHandler`), `Ai/Engineering/GitHubSourceFileSource.cs`, `tests/Scmos.Ai.Checks/SourceChecks.cs`.

**Files modified** — `Ai/Engineering/EngineeringAgent.cs` (the multi-step run; `MaxSourceSteps = 4`, `MaxContextChars = 48000`, `Instructions`, `Context`, `Summarise(SourceAnswer)`), `Ai/ToolExecutor.cs` (`AiDispatchBudget(int)` — one stays the default), `Ai/AiContracts.cs` (`AiProviderRequest.Context`; `AiChatResponse.Source` appended), `Ai/Providers/OpenAiProvider.cs` (excerpts as a labelled user turn, bounded), `Ai/AiOptions.cs`, `Ai/AgentRegistry.cs`, `Ai/ToolRegistry.cs`, `Ai/QueryPolicyGuard.cs`, `Ai/AiAuditRules.cs` (tool `read_source`, modes `list`/`file`), `Ai/AgentOrchestrator.cs`, `Ai/AiServiceRegistration.cs`, `Endpoints/AiChatEndpoints.cs`, `Program.cs` (the contents-API client), `Rules/AiPermissions.cs`; `app/scmos/aiControl.ts` (`SourceAnswer`, strict parser — one of metadata or source, never both), `screens/AiControlTower.tsx` (`SourceCard`: the analysis under its label, then each step — a listing, or the numbered window in a scroll box; prompts); `tests/Scmos.Ai.Checks/SharedContractChecks.cs`, `Program.cs`; `tests/aiControl.test.mjs`, `tests/fixtures/ai-control.mjs`.

**Tests added** — 85 checks (`SourceChecks`, labelled "6b:"): thirty-one policy cases (allowed roots and files; settings, environment, workflow, migration, launch, key and binary files; climbing, absolute, doubled and control-character paths; the length cap), normalisation, the secret mask and ordinary source left alone, the two modes over a fixture tree (order, the root, a settings file unlisted, a window, the default window, the last window, an injected comment returned as text with the key masked), every refusal with a readable reason, invalid arguments, the switch, the audit key; the HTTP source (GET, fixed host, fixed ref, climb refused before requesting, 404 as a refusal, a directory read as a file is null); the registry, the schema (ref/command refused), Administrator-only, the switch beside the agent's, the audit vocabulary and step; the agent's run (list → file → refused file → analysis; the summary the server's; the eight audited events in order as the strict sink appends them; usage summed; excerpts labelled untrusted and the key masked; words without a read → clarification; five reads → the fifth refused with no tool offered; metadata after source refused; a provider-supplied ref refused; operator and carrier refused; audit failure withholds; with the switch off the tool is neither offered nor described); the orchestrator (the reply carries the source read and nothing else; a non-administrator refused). Offline total 741 (was 656); with `--write-local-db --local-db` 855. Web: 24 Control Tower tests (the source reply accepted, eleven malformed shapes refused). Unchanged: 595 Node; `-warnaserror`; tsc; eslint.

**Production enablement** — `AI__EngineeringSourceEnabled=true` on `scmos-api-3936`, by the department, after `AI__EngineeringAgentEnabled`. GitHub's unauthenticated rate limit (60 requests an hour per address) bounds how many reads an hour the department gets; a token, if the department ever wants one, is a separate decision and a separate secret.

**Known risks** — a 400-line window of a 5,000-line file may miss the cause; the model says what to read next and the person asks again (four reads a question). The analysis is prose from a model that has read excerpts, not a verdict: it is labelled so on the screen, in the summary and in the basis, and nothing in SCMOS acts on it.
